import { ComingIn } from "@/components/ComingIn";

export default function Compare() {
  return (
    <ComingIn
      title="Compare"
      issue={30}
      what="Two laps on one distance grid, with the delta trace and 25 minisector gains."
    />
  );
}
