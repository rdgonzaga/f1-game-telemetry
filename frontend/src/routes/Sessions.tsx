import { ComingIn } from "@/components/ComingIn";

export default function Sessions() {
  return (
    <ComingIn
      title="Sessions"
      issue={27}
      what="Saved sessions newest first, and the lap table for the one you pick."
    />
  );
}
